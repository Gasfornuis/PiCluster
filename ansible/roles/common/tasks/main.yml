# ansible/roles/common/tasks/main.yml

- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install required packages
  apt:
    name:
      - curl
      - apt-transport-https
      - ca-certificates
    state: present

# PiOS only: enable cgroups for k3s
- name: Check if /boot/cmdline.txt exists (PiOS)
  stat:
    path: /boot/cmdline.txt
  register: cmdline_file

- name: Read current cmdline.txt
  slurp:
    src: /boot/cmdline.txt
  register: cmdline_content
  when: cmdline_file.stat.exists

- name: Enable cgroups for k3s (required on Raspberry Pi)
  lineinfile:
    path: /boot/cmdline.txt
    backrefs: yes
    regexp: '^(.*)$'
    line: '\1 cgroup_enable=cpuset cgroup_enable=memory cgroup_memory=1'
  when:
    - cmdline_file.stat.exists
    - "'cgroup_memory=1' not in (cmdline_content.content | b64decode)"
  notify: Reboot node

- name: Set hostname dynamically from inventory
  hostname:
    name: "{{ inventory_hostname }}"
