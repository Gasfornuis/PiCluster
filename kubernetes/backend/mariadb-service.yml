---
apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: app
  labels:
    app: mariadb
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
    - port: 3306
      targetPort: 3306
